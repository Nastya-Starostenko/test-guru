<h1>Editing Answer</h1>
<p>
  <%= @answer.question.body %>
</p>

<%= render 'form', answer: @answer %>

<%= link_to t('shared.actions.show'), [:admins, @answer] %> |
<%= link_to t('navigations.back'), admins_question_path(@answer.question)  %>
